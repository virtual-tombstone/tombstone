

lib/
├── importers/      # Data intake (T4P, Wikipedia, etc.)
├── person/         # Core structs (Person, Name, Event)
├── site/           # SSG Data Provider (loader.v)
├── store/          # Disk I/O & Sharding (DiskStore)
└── utils/          # Random strings, slugify, etc.

